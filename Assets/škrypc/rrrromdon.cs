using UnityEngine;
using System.Collections;

public class rrrromdon : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void FixedUpdate () {
		transform.rotation = Random.rotation;
	}
}
